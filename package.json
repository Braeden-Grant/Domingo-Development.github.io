{
  "name": "test",
  "version": "1.0.0",
  "description": "This is a proprietary Flask-based web application that showcases a portfolio, handles user reviews, and includes a contact form that sends emails via HMailServer.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "twilio": "^5.3.3"
  }
}
